<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true">

    <!-- Nửa trên: canvas quỹ đạo -->
    <com.example.gnssandopticalflowapp.screen.test.TrajectoryCanvasView
        android:id="@+id/trajectoryCanvas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/cameraHalf"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5" />

    <!-- Nửa dưới: camera + optical flow overlay -->
    <FrameLayout
        android:id="@+id/cameraHalf"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@android:color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5">

        <androidx.camera.view.PreviewView
            android:id="@+id/cameraView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ImageView
            android:id="@+id/cameraOutputOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:importantForAccessibility="no" />

        <TextView
            android:id="@+id/tvFlowInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_margin="10dp"
            android:background="#99000000"
            android:paddingHorizontal="10dp"
            android:paddingVertical="5dp"
            android:text="KLT"
            android:textColor="@android:color/white"
            android:textSize="11sp" />
    </FrameLayout>

    <!-- HUD: thời gian / quãng đường / số điểm dừng -->
    <TextView
        android:id="@+id/tvTrackInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:background="#CC1A1A2E"
        android:paddingHorizontal="14dp"
        android:paddingVertical="7dp"
        android:text="0.0 m  •  00:00  •  0 stops"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Nút recenter canvas -->
    <ImageView
        android:id="@+id/btnRecenterCanvas"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_margin="12dp"
        android:background="#CC1A1A2E"
        android:contentDescription="Recenter canvas"
        android:padding="8dp"
        android:src="@drawable/ic_current_location"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Nút Start / End tracking, nổi giữa đường chia đôi -->
    <TextView
        android:id="@+id/btnStartEnd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_gradient_update_button_12"
        android:paddingHorizontal="34dp"
        android:paddingVertical="11dp"
        android:text="Start"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/cameraHalf"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/cameraHalf" />

</androidx.constraintlayout.widget.ConstraintLayout>
